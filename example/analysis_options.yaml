include: package:lints/recommended.yaml

analyzer:
  exclude:
    # env.dart depends on env.g.dart which is generated by envied_generator
    # via build_runner. Until a developer creates a .env file and runs
    # build_runner, this file will have unresolvable errors. Exclude it
    # so it doesn't pollute the workspace analyzer.
    - lib/env/**
