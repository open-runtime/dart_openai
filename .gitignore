# Files and directories created by pub.
.dart_tool/
.packages

build/

# Example build artifacts and secrets (not the example source itself)
example/.dart_tool/
example/.env
example/build/

*.jsonl
env.g.dart
.env
*.mp3
*.png
.idea/
